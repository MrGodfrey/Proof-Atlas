On the controlled subinterval $(a_j,a_j+T_j)$ in [[main.proof.lr_iteration]], [[main.claim.partial_null_control]] gives a control $u_j$ such that

$$
P_j y(a_j+T_j)=0,
\qquad
\mathbb{P}\text{-a.s.}
$$

This cancellation is the hypothesis needed to apply [[main.claim.free_decay]] on the following uncontrolled subinterval.
